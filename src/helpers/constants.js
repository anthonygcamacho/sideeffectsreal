const path = require('path');

module.exports = {
    DIST_DIR_PATH: path.join(__dirname, '../../dist'),
    VIEW_PATH: path.join(__dirname, '../../templates/pages'),
    PARTIALS_PATH: path.join(__dirname, '../../templates/partials')
}